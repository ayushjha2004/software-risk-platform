# scripts/

Reserved for developer/operations helper scripts (e.g. database
seeding, dataset download, deployment helpers) added in later phases.

No scripts are required for Phase 1; use the root `Makefile` for
common commands.
