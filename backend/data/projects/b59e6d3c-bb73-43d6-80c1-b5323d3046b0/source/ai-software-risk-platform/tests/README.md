# tests/

Reserved for cross-cutting / end-to-end tests that span both the
backend and frontend (e.g. full-stack integration tests), added in a
later phase.

Phase 1 unit and integration tests live alongside each service:

- Backend tests: `backend/tests/`
- Frontend tests: `frontend/tests/`
